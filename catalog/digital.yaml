name: digital
tools:
  [
    yosys,
    openroad,
    klayout,
    magic,
    netgen,
    verilator,
    gtkwave,
    iverilog,
  ]
# opensta: bundled inside openroad (sta binary), not a separate install
