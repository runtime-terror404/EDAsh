name: digital
tools:
  [
    # ASIC backend (micromamba)
    openroad,
    klayout,
    magic,
    netgen,
    # Synthesis & simulation (oss-cad-suite)
    yosys,
    gtkwave,
    iverilog,
    verilator,
    # FPGA (oss-cad-suite)
    nextpnr,
    icestorm,
    prjtrellis,
    openfpgaloader,
    # Formal (oss-cad-suite)
    sby,
    boolector,
    z3,
  ]
# opensta: bundled inside openroad (sta binary)
